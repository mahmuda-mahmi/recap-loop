var works = ['module unlock on 8', 'watch videos and practice', 'take notes while watching', 'practice on your own after watching module', 'problem aries', 'join support session'];

for(i= 0; i < works.length; i++){
     var work = works[i];
     console.log(work);
}
